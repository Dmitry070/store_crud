# ============================================
# Dockerfile — Инструкция по сборке образа PHP
# ============================================
# Берём готовый образ PHP 8.2 с FPM
FROM php:8.2-fpm

# ── Устанавливаем расширение PDO для MySQL ──
# pdo_mysql — именно через него PHP общается с MySQL
# docker-php-ext-install — специальная команда для образов PHP
RUN docker-php-ext-install pdo_mysql

# ── Устанавливаем рабочую директорию ──
# Все команды внутри контейнера будут выполняться из этой папки
WORKDIR /var/www/html
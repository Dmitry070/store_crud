# ── IDE (PhpStorm, VS Code) ──
.idea/
.vscode/

# ── Системные файлы ──
.DS_Store
Thumbs.db

# ── Docker: данные MySQL хранятся в томе, не в файлах ──
# (тут нечего игнорировать, но на всякий случай)

# ── Логи ──
*.log